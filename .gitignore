# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node
node_modules/
dist/
*.local

# Environment — never commit real config
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.vscode/
.idea/
*.swp

# Build caches
.tsbuildinfo
frontend/node_modules/.tmp/

# Router audit log (runtime artefact)
backend/logs/

# Embedded vector store (rebuild with scripts/ingest_samples.py)
backend/data/chroma/

# Uploaded files (runtime artefact)
backend/data/uploads/

# Generated deliverables (runtime artefact)
backend/data/generated/

# Conversation transcripts and user memory (runtime, private)
backend/data/memory.db
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
